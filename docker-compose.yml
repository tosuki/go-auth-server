version: '3.1'

services:
  db:
    container_name: postgres_go_auth_server
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: root
      POSTGRES_DB: auth_server
    ports:
      - "5432:5432"